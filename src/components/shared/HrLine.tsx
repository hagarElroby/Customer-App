const HrLine = ({ className }: { className?: string }) => {
  return (
    <div
      className={`${className} border hd:border-[0.1vh] w-full border-customBorder`}
    />
  );
};
export default HrLine;
